# 2026-09-09

## Goal

Prepare the repository for fast, reproducible startup in fresh Amp orbs.

## Decisions

- Use `uv sync --frozen --extra dev` as the single installation path because `uv.lock` pins Python 3.12 and all runtime and development dependencies.
- Do not create `.env`: empty values in the template would override application defaults, and cloud credentials must be supplied through the orb environment rather than interactive setup.
- Do not start PostgreSQL or Redis because the current documented extraction workflow and offline test suite do not require them.

## Files changed

- Added executable `.agents/setup` and `.agents/resume` lifecycle scripts.
- Ignored `.amp/portals/*` metadata.

## Verification

- Cold setup: 11.14 seconds; warm setup: 1.28 seconds; resume: under 0.01 seconds.
- Clean login shell resolved uv, Python 3.12.14, and native project dependencies.
- `uv run --frozen pytest pipeline/tests -q`: 408 passed.

## Next steps

- Push the setup commit to the default branch so future project orbs activate it.
