# 2026-09-13 — Current pipeline research briefing

## Goal

Produce a durable, research-agent-ready evidence briefing for the current Meta Ads model/data and image-generation pipelines without merging or modifying `master`.

## Decisions and work

- Created documentation branch `docs/current-pipelines-research-brief` from shared base `b0951916ff24981a218247606f9fa29d410a5a07`.
- Retrieved and inspected full Changes captures from local-only model branch `auto/meta-ads-model-pipeline` and generation branch `feat/generation-quality`; messages were not treated as file transfer.
- Downloaded and recomputed hashes for the blank taxonomy review, baseline observation window, and corrected v3 bundle-v2 manifest.
- Reconciled final clean source heads `a013f8c5d87e512788af616972c23562b82bf29d` and `db9908fab4d70a2638d9ac1f75d6a06b2246f9c2`, including complete ordered commit inventories and recorded test evidence.
- Added `docs/current-meta-model-and-generation-pipelines-research-brief.md`, separating verified findings, offline structural evidence, blocked work, and proposals. It includes architecture, handoff and fail-closed contracts, P0–P5 gates, and 15 answerable research investigations.
- Preserved the non-contamination rule: generated candidates, defects, and reviewer labels cannot become Meta proxy labels.

## Files changed

- `docs/current-meta-model-and-generation-pipelines-research-brief.md`
- `Vault/Sessions/Daily/2026-09-13.md`

## Verification

- Recomputed transferred artifact SHA-256 values and inspected the corrected bundle manifest roles, schemas, source provenance, and file hashes.
- Confirmed the taxonomy artifact contains 120 items and zero labels.
- Reconstructed both feature trees from their common base and transferred deltas; independently reproduced model pipeline 483 tests, ingestion 446 tests, generation pipeline 536 tests, and generation extraction/CLI 15 tests. Changes transfer required restoring two model shell-helper executable bits before its final run.
- Confirmed the briefing contains 30 model commits, 31 generation commits, 15 research investigations, balanced fenced blocks, and both exact feature-branch heads.
- `git diff --check` passed.

## Next steps

- Complete the human taxonomy and rights-approved benchmark/asset inputs before paid or promotional work.
- Preserve v3 as global fallback; add product-enrichment gate enforcement only in a future shadow v4 loader.
- Evaluate v3 unchanged on a genuinely later mature window before retraining.

---

# 2026-09-13 — Product-site evidence restored to Supplements training

## Goal

Restore price, rating, product-description, review-volume, subscription, and variant evidence from
ad landing pages without bypassing taxonomy, temporal evaluation, provenance, or Generation's v3
contract.

## Decisions and work

- Kept the sequence human taxonomy → free Shopify/structured pass → paid ZenRows backfill → coverage
  gate → Extraction/matrix/training. The separate LLM advertorial fallback remains explicit and off.
- Accepted both `ZENROWS_API_KEY` and the owner-configured `ZENROWS_API` through `pipeline.config`.
- Added atomic JSON/CSV checkpoints and fail-fast locks to every product-enrichment CLI path.
- Added fixed product-page (50%), price (40%), and description/USP (25%) coverage gates, content
  hashing, extraction-method counts, and per-field model support at 100 rows/10 advertisers.
- Added rating count (log1p), product description/USP lengths and presence, rating presence, and
  subscription status to the model matrix. Existing categorical price tier remains a model feature;
  raw price remains excluded because currency and bundle units are not comparable.
- Embedded enrichment evidence in training and promotion reports. Future-window scoring remains
  possible for frozen older models, but promotion fails closed when the baseline has no product gate.
- Added a Generation-side guard that removes rating/price-tier directives from new reports when the
  relevant product field lacks row/advertiser support. Immutable v3 behavior and files are unchanged.
- Preserved loop isolation: product metadata is explanatory input; generated/reviewer data never
  becomes a Meta success label.

## Verification

- `uv run pytest pipeline/tests -q` — 483 passed; two pre-existing SciPy precision warnings.
- `uv run pytest ingestion/tests -q` — 446 passed.
- Focused product/workflow/enrichment suite — 176 passed.
- Changed-path Ruff (excluding pre-existing E501 debt) and `git diff --check` passed.
- After `amp orb restart-processes`, the settings-only health check confirmed ZenRows configuration.
  Two isolated one-URL paid smoke calls verified authenticated HTTP 200 transport and checkpoints:
  one page yielded no supported fields; one `/products/` page yielded description, rating, and review
  count but no price. Temporary smoke data was deleted and never entered training. This validates the
  free structured-first ordering and the need for field-specific coverage gates; it is not a corpus
  coverage or model-performance claim.

## Next steps

1. Complete and adjudicate the blank 120-ad Supplements taxonomy review.
2. Run the restartable enrichment workflow, inspect diagnostics and the coverage report, then sync
   approved enriched rows to GCP.
3. Freeze an untouched later window and evaluate the old model before fitting or handing off any v4
   challenger.
