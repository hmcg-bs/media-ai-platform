"""Model-ready matrix construction, data-quality flagging, and per-target
XGBoost training for the Step 3 feature matrix (pipeline/feature_engineering).

Distinct from pipeline/models/ (the Step 2 Pydantic Master Schema) — this
package deals with trained ML models, not extraction schemas.
"""
